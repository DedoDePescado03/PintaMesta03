<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="PintaMesta.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:PintaMesta"
    Shell.FlyoutBehavior="Disabled"
    Title="PintaMesta">

    <ShellContent
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />

    <ShellContent 
        ContentTemplate="{DataTemplate local:DrawingPage}" 
        Route="DrawingPage"/>

    <ShellContent 
        Title="Lobby" 
        ContentTemplate="{DataTemplate local:LobbyPage}" 
        Route="LobbyPage"/>

    <ShellContent 
        ContentTemplate="{DataTemplate local:GuessingPage}" 
        Route="GuessingPage"/>

</Shell>
